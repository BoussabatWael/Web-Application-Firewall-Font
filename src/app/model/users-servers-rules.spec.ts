import { UsersServersRules } from './users-servers-rules';

describe('UsersServersRules', () => {
  it('should create an instance', () => {
    expect(new UsersServersRules()).toBeTruthy();
  });
});
