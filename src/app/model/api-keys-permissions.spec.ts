import { ApiKeysPermissions } from './api-keys-permissions';

describe('ApiKeysPermissions', () => {
  it('should create an instance', () => {
    expect(new ApiKeysPermissions()).toBeTruthy();
  });
});
